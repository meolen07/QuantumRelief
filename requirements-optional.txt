# Optional local extras beyond Cloud requirements.txt
# Pitch deck generator (scripts/generate_pitch_deck.py):
python-pptx>=1.0,<1.1
# Note: pennylane is now in requirements.txt for Hybrid QML on Cloud.
